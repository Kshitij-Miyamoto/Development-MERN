const score = 12 ; 
const balance = new Number(100)
console.log(balance);
console.log(balance.toString().length);
console.log(balance.toFixed(1));

const hunereds = 1000000000 ;
console.log(hunereds.toLocaleString());

Math.abs(-10);
Math.round(10.5);  // 11
Math.round(10.4);  // 10
Math.ceil(10.1);  // 11
Math.floor(10.9);  // 10
Math.min(10, 5, 20);  // 5
Math.max(10, 5, 20);  // 20
Math.pow(2, 3);  // 8 (2 raised to the power of 3)
Math.sqrt(16);  // 4
Math.random();  // Random number between 0 and 1
Math.log(1);  // 0 (ln(1) = 0)
Math.log(Math.E);  // 1 (ln(e) = 1)
Math.log10(100);  // 2 (log10(100) = 2)
Math.sin(Math.PI / 2);  // 1 (sin(90°) = 1)
Math.cos(0);  // 1 (cos(0°) = 1)
Math.tan(Math.PI / 4);  // 1 (tan(45°) = 1)
console.log(Math.PI);  // 3.141592653589793
console.log(Math.E);  // 2.718281828459045
